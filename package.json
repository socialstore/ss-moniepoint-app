{
  "name": "ss-moniepoint-app",
  "private": true,
  "type": "module",
  "workspaces": ["api", "ui"],
  "scripts": {
    "dev": "bun run --filter '@ss-moniepoint/api' dev",
    "build:ui": "bun run --filter '@ss-moniepoint/ui' build",
    "typecheck": "tsc --noEmit",
    "test": "bun test ./api"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/bun": "^1.1.14"
  }
}
